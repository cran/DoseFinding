to do: 
- can only use one linear model in Mods (what if we have two different
  max effects?)!
- allow for dose estimate with active control  
- function to interactively choose guesstimates (maybe use the slider
  function from the TeachingDemos package and wrap the plotModels
  function in it).
